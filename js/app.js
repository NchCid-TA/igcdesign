var I18N_EN = {"nav.video": "Video", "nav.design": "Design", "nav.games": "Videogames", "nav.contact": "Contact", "home.eyebrow": "<span class=\"tick\">＋</span> MULTIDISCIPLINARY PORTFOLIO", "home.roleline": "Graphic design, video, videogames and <b>web design</b> — this very site is the fourth piece of the portfolio.", "home.tag.video": "VIDEO", "home.tag.design": "GRAPHIC DESIGN", "home.tag.games": "VIDEOGAMES", "home.disc.title": "Disciplines", "home.disc.note": "Four fields, one visual system — go into each one to see the work.", "home.tile.video.name": "Video", "home.tile.video.sub": "Editing &amp; motion", "home.tile.design.name": "Design", "home.tile.design.sub": "Branding &amp; editorial", "home.tile.games.name": "Videogames", "home.tile.games.sub": "Design &amp; prototyping", "home.tile.web.sub": "This very site", "home.about.title": "About me", "home.about.p1": "I’m Ignacio González Cid, a multidisciplinary designer and creator. I move between video editing, graphic identity, videogame design and web development — four different languages that share the same way of looking at things: composition, rhythm and detail.", "home.about.p2": "This site is, in itself, the <b>web design</b> piece of the portfolio.", "nav.backhome": "← Home", "video.title": "Video", "video.desc": "Editing, color grading and motion graphics — from short films to brand pieces.", "video.credit": "Filmed and edited by Ignacio González.", "video.long.title": "Long-form videos", "video.long.note": "Reports and pieces for Real Sporting de Gijón.", "video.disclaimer": "All videos published in this section were filmed and edited by me.", "video.soon": "Coming soon — TikToks section.", "design.title": "Graphic design", "design.desc": "Brand identity, editorial and packaging — from the grid to the printed piece.", "design.featured.eyebrow": "REAL PROJECT", "design.featured.desc": "A complete visual identity for a streaming show about Real Sporting de Gijón: football with a laid-back tone and a diverse, close-knit community behind the screen.", "design.tag.identity": "IDENTITY", "link.viewproject": "View project →", "more.soon": "More projects coming soon.", "nav.backdesign": "← Design", "tribunona.desc": "La Tribunona is a streaming show and podcast about Real Sporting de Gijón: football with a laid-back tone and a diverse, close-knit community. This project brings together the whole visual identity of the Twitch channel — stream scenes, transitions, logo and graphics for social media.", "tribunona.tag.brand": "BRAND IDENTITY", "tribunona.scenes.title": "Scenes", "tribunona.scenes.note": "Full set of OBS scenes, in pairs, at their original resolution.", "tribunona.cap.intro": "Intro", "tribunona.cap.offline": "Offline", "tribunona.cap.dual": "Stream — dual camera", "tribunona.cap.triple": "Stream — triple camera", "tribunona.cap.quad": "Stream — quad camera", "tribunona.cap.dualshare": "Dual — screen share", "tribunona.cap.tripleshare": "Triple — screen share", "tribunona.cap.end": "Stream end", "tribunona.cap.community": "Graphic — community (Twitter)", "tribunona.cap.logo": "Logo", "games.title": "Videogames", "games.desc": "Level design, environment art and prop modelling.", "games.tech.note": "Technical environments built with procedural generation and dynamic lighting.", "games.village.desc": "Procedural village built with PCG and SpeedTree vegetation.", "games.resilience.desc": "Mountain forest environment with dynamic lighting.", "games.props.title": "Props", "games.props.note": "Modelling, texturing and lookdev of individual props.", "games.doorstudy.eyebrow": "TEXTURING · LOOKDEV", "games.doorstudy.desc": "Garage, motel and school doors, in two variants each.", "games.revolver.desc": "Hard-surface modelling and texturing in Marmoset Toolbag.", "games.smartmat.desc": "Smart materials applied to outdoor furniture.", "nav.backgames": "← Videogames", "village.eyebrow": "<span class=\"tick\">＋</span> UNREAL ENGINE 5 · PCG · ENVIRONMENT", "village.desc": "A procedurally generated village: landscape, vegetation and paths built with Unreal Engine 5’s PCG system, combined with SpeedTree assets for the vegetation.", "tag.environment": "ENVIRONMENT", "block.gallery": "Gallery", "village.process.title": "Process — PCG", "village.process.note": "Five Procedural Content Generation techniques used to build the environment.", "village.vid.landscape": "Automatic landscape adjustment", "village.vid.cliff": "Cliff variation", "village.vid.dressing": "Set dressing", "village.vid.paths": "Paths — spline mesh", "village.vid.trees": "Tree spawner", "resilience.desc": "A videogame environment set in a mountain forest, with a wooden watchtower as its focal point — an environment art piece with dynamic lighting conditions.", "resilience.tag.lighting": "DYNAMIC LIGHTING", "resilience.vid.label": "Preview — dynamic environment", "doorstudy.eyebrow": "<span class=\"tick\">＋</span> TEXTURING · LOOKDEV · PROPS", "doorstudy.desc": "A texturing study of industrial doors — garage, motel and school — each in two variants.", "tag.texturing": "TEXTURING", "doorstudy.cap.garage1": "Garage — 01", "doorstudy.cap.garage2": "Garage — 02", "doorstudy.cap.motel1": "Motel — 01", "doorstudy.cap.motel2": "Motel — 02", "doorstudy.cap.school1": "School — 01", "doorstudy.cap.school2": "School — 02", "revolver.desc": "Hard-surface modelling and texturing of a revolver, rendered in Marmoset Toolbag from two angles.", "revolver.cap.1": "Render — 01", "revolver.cap.2": "Render — 02", "smartmat.eyebrow": "<span class=\"tick\">＋</span> SMART MATERIALS · TEXTURING", "smartmat.desc": "Smart materials developed for outdoor furniture — a deck chair, a barbecue and a picnic table — with a sphere material sample and the final application on the props.", "smartmat.cap.sample": "Material sample", "smartmat.cap.applied": "Applied — outdoor furniture", "web.title": "Web design", "web.desc": "Interface design and development — this site included.", "web.molyn.eyebrow": "REAL PROJECT — UNPUBLISHED", "web.molyn.featureddesc": "Site for Molyn (Torreón, MX), a 3D animation, virtual and augmented reality studio. Interactive 3D logo built with Three.js, GSAP/Lenis scroll animation, and a form that builds the WhatsApp message automatically.", "web.asturias.eyebrow": "LIVE ↗", "web.asturias.desc": "Interactive map of Asturias — restaurants, cider houses, routes and more.", "web.thissite.eyebrow": "THIS SITE", "web.thissite.desc": "Identity, home page and pages by discipline.", "nav.backweb": "← Web", "molyn.desc": "Site for Molyn (Torreón, MX), a 3D animation, virtual and augmented reality studio. Interactive 3D logo, animated scroll and a form that builds the WhatsApp message automatically — without relying on any third-party external service.", "molyn.disclaimer": "Unpublished project — screenshots and video taken from the local development environment.", "molyn.vid.label": "Site walkthrough", "contact.eyebrow": "<span class=\"tick\">＋</span> AVAILABLE FOR PROJECTS", "contact.title": "Contact", "contact.desc": "Video, graphic design, videogames or web? Let’s talk.", "contact.intro": "You can write to me directly, find me on these sites, or download my CV.", "contact.linkedin.tag": "PROFESSIONAL NETWORK", "contact.artstation.tag": "3D PORTFOLIO", "contact.email.tag": "DIRECT", "contact.email.sub": "Open mail client", "contact.cv.btn": "Download CV ↓", "contact.cv.note": "PDF · a quick look at my profile.", "contact.form.name": "Name", "contact.form.email": "Your email", "contact.form.message": "Message", "contact.form.submit": "Send by email", "contact.form.note": "This will open your email client with the message already written, ready to send to my two accounts.", "footer.contact": "Contact — <a href=\"mailto:ngc3dmodeler@gmail.com\">ngc3dmodeler@gmail.com</a>"};
  var I18N_ES = {};
  document.querySelectorAll('[data-i18n]').forEach(function(el){
    I18N_ES[el.dataset.i18n] = el.innerHTML;
  });
  var currentLang = localStorage.getItem('igc-lang') || 'es';

  function applyLang(lang){
    currentLang = lang;
    var dict = lang === 'en' ? I18N_EN : I18N_ES;
    document.querySelectorAll('[data-i18n]').forEach(function(el){
      var key = el.dataset.i18n;
      if (dict[key] !== undefined) el.innerHTML = dict[key];
    });
    document.documentElement.lang = lang;
    document.querySelectorAll('[data-lang-btn]').forEach(function(btn){
      btn.classList.toggle('active', btn.dataset.langBtn === lang);
    });
    var cvLink = document.getElementById('cv-download');
    if (cvLink) {
      if (lang === 'en') {
        cvLink.href = 'assets/cv/CV_Ignacio_Gonzalez_Cid_EN.pdf';
        cvLink.setAttribute('download', 'CV_Ignacio_Gonzalez_EN.pdf');
      } else {
        cvLink.href = 'assets/cv/CV_Ignacio_Gonzalez_Cid_ES.pdf';
        cvLink.setAttribute('download', 'CV_Ignacio_Gonzalez.pdf');
      }
    }
    try { localStorage.setItem('igc-lang', lang); } catch(e) {}
  }
  document.querySelectorAll('[data-lang-btn]').forEach(function(btn){
    btn.addEventListener('click', function(){
      if (btn.dataset.langBtn !== currentLang) applyLang(btn.dataset.langBtn);
    });
  });
  applyLang(currentLang);

  var menuToggle = document.getElementById('menu-toggle');
  var navRight = document.getElementById('nav-right');
  function closeMobileMenu(){
    if (!menuToggle || !navRight) return;
    navRight.classList.remove('mobile-open');
    menuToggle.classList.remove('is-open');
    menuToggle.setAttribute('aria-expanded', 'false');
  }
  if (menuToggle && navRight) {
    menuToggle.addEventListener('click', function(){
      var open = navRight.classList.toggle('mobile-open');
      menuToggle.classList.toggle('is-open', open);
      menuToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
    });
    navRight.querySelectorAll('a, .lang-switch button').forEach(function(el){
      el.addEventListener('click', closeMobileMenu);
    });
    document.addEventListener('keydown', function(e){
      if (e.key === 'Escape') closeMobileMenu();
    });
  }

  function applyRoute(){
    var hash = location.hash.replace('#/','') || 'home';
    var pages = document.querySelectorAll('.page');
    for(var i=0;i<pages.length;i++){
      pages[i].hidden = pages[i].dataset.route !== hash;
    }
    var links = document.querySelectorAll('[data-route-link]');
    for(var j=0;j<links.length;j++){
      links[j].classList.toggle('active', links[j].dataset.routeLink === hash);
    }
    var active = document.querySelector('.page[data-route="'+hash+'"]');
    if(active){
      var heading = active.querySelector('h1,h2');
      if(heading){
        heading.setAttribute('tabindex','-1');
        heading.focus({preventScroll:true});
      }
    }
    window.scrollTo(0,0);
  }

  var lazyVideoObserver = 'IntersectionObserver' in window ? new IntersectionObserver(function(entries){
    entries.forEach(function(entry){
      var v = entry.target;
      if (entry.isIntersecting) {
        if (v.dataset.src) {
          v.autoplay = true;
          v.src = v.dataset.src;
          v.removeAttribute('data-src');
          v.load();
        }
        setTimeout(function(){ v.play().catch(function(){}); }, 30);
      } else if (!v.paused) {
        v.pause();
      }
    });
  }, { threshold: 0.25 }) : null;

  document.querySelectorAll('video[data-src]').forEach(function(v){
    if (lazyVideoObserver) {
      lazyVideoObserver.observe(v);
    } else {
      v.autoplay = true;
      v.src = v.dataset.src;
      v.removeAttribute('data-src');
      v.load();
      v.play().catch(function(){});
    }
  });

  var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var pageTransition = document.querySelector('.page-transition');

  function navigate(){
    if(reduceMotion || !pageTransition){ applyRoute(); return; }
    pageTransition.classList.add('is-active');
    setTimeout(function(){
      applyRoute();
      setTimeout(function(){
        pageTransition.classList.remove('is-active');
      }, 160);
    }, 620);
  }
  window.addEventListener('hashchange', navigate);
  if (reduceMotion) {
    document.querySelectorAll('.video-tile video').forEach(function(v){
      v.removeAttribute('autoplay'); v.removeAttribute('loop'); v.pause(); v.setAttribute('controls','');
    });
  }
  applyRoute();

  var lightbox = document.createElement('div');

  var contactForm = document.getElementById('contact-form');
  if (contactForm) {
    contactForm.addEventListener('submit', function(e){
      e.preventDefault();
      var name = document.getElementById('cf-name').value;
      var email = document.getElementById('cf-email').value;
      var message = document.getElementById('cf-message').value;
      var isEn = currentLang === 'en';
      var subject = encodeURIComponent((isEn ? 'Portfolio contact — ' : 'Contacto desde el portfolio — ') + name);
      var body = encodeURIComponent((isEn ? 'Name: ' : 'Nombre: ') + name + '\nEmail: ' + email + '\n\n' + message);
      window.location.href = 'mailto:ngc3dmodeler@gmail.com,igcdsign@gmail.com?subject=' + subject + '&body=' + body;
    });
  }

  lightbox.className = 'lightbox-overlay';
  var lightboxImg = document.createElement('img');
  var lightboxVideo = document.createElement('video');
  lightboxVideo.muted = true;
  lightboxVideo.loop = true;
  lightboxVideo.playsInline = true;
  lightboxVideo.controls = true;
  lightbox.appendChild(lightboxImg);
  lightbox.appendChild(lightboxVideo);
  document.body.appendChild(lightbox);

  function closeLightbox(){
    lightbox.classList.remove('is-open');
    document.body.style.overflow = '';
    lightboxVideo.pause();
  }
  function openImage(src, alt){
    lightboxVideo.style.display = 'none';
    lightboxVideo.pause();
    lightboxImg.style.display = 'block';
    lightboxImg.src = src;
    lightboxImg.alt = alt || '';
    lightbox.classList.add('is-open');
    document.body.style.overflow = 'hidden';
  }
  function openVideo(src){
    lightboxImg.style.display = 'none';
    lightboxVideo.style.display = 'block';
    lightboxVideo.src = src;
    lightbox.classList.add('is-open');
    document.body.style.overflow = 'hidden';
    lightboxVideo.play().catch(function(){});
  }
  lightbox.addEventListener('click', function(e){
    if (e.target === lightboxVideo) return;
    closeLightbox();
  });
  document.addEventListener('keydown', function(e){
    if (e.key === 'Escape') closeLightbox();
  });
  document.querySelectorAll('.gallery img, .featured-media img, .detail-hero-media img').forEach(function(el){
    el.addEventListener('click', function(){ openImage(el.src, el.alt); });
  });
  document.querySelectorAll('.video-tile video').forEach(function(el){
    el.addEventListener('click', function(){ openVideo(el.currentSrc || el.src); });
  });

  // ---------- interactive scanner background: dot grid + trailing glow ----------
  (function(){
    var canvas = document.getElementById('bgcanvas');
    if (!canvas) return;
    var ctx = canvas.getContext('2d');
    var ambientSvg = document.querySelector('.ambient svg');
    var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    var W = 0, H = 0, DPR = Math.min(window.devicePixelRatio || 1, 2);
    var GAP = 42;
    var dots = [];

    function layout(){
      W = window.innerWidth;
      H = window.innerHeight;
      canvas.width = W * DPR;
      canvas.height = H * DPR;
      canvas.style.width = W + 'px';
      canvas.style.height = H + 'px';
      ctx.setTransform(DPR, 0, 0, DPR, 0, 0);

      dots = [];
      var cols = Math.ceil(W / GAP) + 1;
      var rows = Math.ceil(H / GAP) + 1;
      for (var r = 0; r < rows; r++) {
        for (var c = 0; c < cols; c++) {
          dots.push({ x: c * GAP, y: r * GAP, phase: Math.random() * Math.PI * 2 });
        }
      }
    }

    var pointer = { x: -9999, y: -9999, active: false };
    var glow = { x: W / 2, y: H / 2 };
    var t = 0;
    var RADIUS = 75;

    window.addEventListener('pointermove', function(e){
      pointer.x = e.clientX;
      pointer.y = e.clientY;
      pointer.active = true;
    });
    window.addEventListener('pointerleave', function(){ pointer.active = false; });
    window.addEventListener('resize', layout);

    function frame(){
      t += 0.016;

      var targetX, targetY;
      if (pointer.active) {
        targetX = pointer.x;
        targetY = pointer.y;
      } else {
        targetX = W / 2 + Math.cos(t * 0.35) * (W * 0.28);
        targetY = H / 2 + Math.sin(t * 0.5) * (H * 0.22);
      }

      if (reduceMotion) {
        glow.x = W / 2;
        glow.y = H / 2;
      } else {
        glow.x += (targetX - glow.x) * 0.16;
        glow.y += (targetY - glow.y) * 0.16;
      }

      // dots react to the real cursor instantly; only the halo trails behind
      var reactX = pointer.active ? pointer.x : glow.x;
      var reactY = pointer.active ? pointer.y : glow.y;

      ctx.clearRect(0, 0, W, H);

      var grad = ctx.createRadialGradient(glow.x, glow.y, 0, glow.x, glow.y, 130);
      grad.addColorStop(0, 'rgba(200,255,61,0.10)');
      grad.addColorStop(0.5, 'rgba(200,255,61,0.035)');
      grad.addColorStop(1, 'rgba(200,255,61,0)');
      ctx.fillStyle = grad;
      ctx.fillRect(0, 0, W, H);

      for (var i = 0; i < dots.length; i++) {
        var d = dots[i];
        var dx = d.x - reactX;
        var dy = d.y - reactY;
        var dist = Math.sqrt(dx * dx + dy * dy);
        var proximity = Math.max(0, 1 - dist / RADIUS);

        var idle = reduceMotion ? 0.5 : 0.5 + Math.sin(t * 0.8 + d.phase) * 0.5;
        var baseAlpha = 0.10 + idle * 0.10;
        var alpha = baseAlpha + proximity * 0.85;
        var size = 1.1 + proximity * 1.6;

        if (proximity > 0.02) {
          var mix = proximity;
          var cr = Math.round(46 + (200 - 46) * mix);
          var cg = Math.round(49 + (255 - 49) * mix);
          var cb = Math.round(54 + (61 - 54) * mix);
          ctx.fillStyle = 'rgba(' + cr + ',' + cg + ',' + cb + ',' + Math.min(alpha, 1) + ')';
        } else {
          ctx.fillStyle = 'rgba(46,49,54,' + Math.min(alpha, 1) + ')';
        }

        ctx.beginPath();
        ctx.arc(d.x, d.y, size, 0, Math.PI * 2);
        ctx.fill();
      }

      if (ambientSvg && !reduceMotion) {
        var cx = W / 2, cy = H / 2;
        var px = (glow.x - cx) / cx;
        var py = (glow.y - cy) / cy;
        ambientSvg.style.transform = 'translate(' + (px * 14) + 'px,' + (py * 10) + 'px)';
      }

      requestAnimationFrame(frame);
    }

    layout();
    frame();
  })();
